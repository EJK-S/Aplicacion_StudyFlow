name: flutter_studyflow
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  
  # Gestión de estado
  flutter_riverpod: ^2.5.1
  
  # Utilidades
  flutter_form_builder: ^9.1.1
  cupertino_icons: ^1.0.8

  # 👇 CAMBIO: Usamos Hive (Base de datos blindada)
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Otras dependencias

  metadata_fetch: ^0.4.2
  http: ^1.2.0
  html: ^0.15.4

  pdf: ^3.10.4
  printing: ^5.11.1

  path_provider: ^2.1.2
  share_plus: ^10.0.0
  file_picker: ^8.0.0
  intl: ^0.19.0
  fl_chart: ^1.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.8
  
  # Generador de Hive
  hive_generator: ^2.0.1

  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21
  web:  # <--- AGREGA ESTO
    generate: true
    image_path: "assets/icon/icon.png"
    background_color: "#141E30" # El color de fondo de tu app
    theme_color: "#141E30"